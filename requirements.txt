# Production dependencies. Pinned for reproducible installs.
# Lives at the repo root because Vercel installs from this path and its parser
# does not follow "-r" includes. Dev-only tools go in requirements-dev.txt.

Django==6.1
djangorestframework==3.18.0

# Sends the CORS headers the browser needs to let the frontend call this API
django-cors-headers==4.9.0

# Loads .env into environment variables
python-dotenv==1.2.3

# HTTP client for model API calls
httpx==0.28.1
